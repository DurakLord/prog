drop database chrono;
create database chrono;
use chrono;
create table customers(
    id int,
    name varchar(255),
    cache int,
    job varchar(255),
    habit varchar(255)
);



select * from customers;
